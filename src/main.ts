/**
 * Minimal description of what the module does.
 * @module
 * @remarks
 * - Write markdown here.
 */

// import primitives
import console from "node:console";

/**
 * describe on a function by function basis
 */
const a: string = `ready to run typescript, add babel for building or rollup for bundling if needed.`;

console.log(a);